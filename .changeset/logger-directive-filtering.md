---
"@prosopo/logger": minor
---

Add directive-based scope filtering via PROSOPO_LOG_LEVEL and subscope support to with(). PROSOPO_LOG_LEVEL now accepts comma-separated directives like "warn,database=trace" to set per-scope log levels.
