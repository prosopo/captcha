---
"@prosopo/types": patch
---

Add unit and type tests for the shared types: request sanitisation, client settings defaults, test site keys, captcha dataset types, decision-machine counters and procaptcha tokens.
