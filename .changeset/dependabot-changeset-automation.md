---
---

ci: enable Dependabot (npm + github-actions) and auto-generate a changeset + lockfile refresh on each Dependabot PR so the `changesets` check passes. Empty changeset: CI-only change, no package bump.
