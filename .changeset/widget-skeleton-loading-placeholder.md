---
"@prosopo/widget-skeleton": patch
---

While the widget loads, show a pulsing box the size and shape of the checkbox instead of a spinning ring. A spinner now only means a check is in progress. The placeholder takes the same space as before, so nothing moves when the real checkbox arrives, and it stops pulsing for visitors who ask for reduced motion.
