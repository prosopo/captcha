---

---

ci: add pinned-versions supply-chain workflow and pin existing dependency ranges to their already-resolved exact versions. No release needed — installs are identical (package-lock.json already resolved to these versions).
