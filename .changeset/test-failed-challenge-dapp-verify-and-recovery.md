---
"@prosopo/provider": patch
---

Extend the failed image captcha integration test to assert the dapp verifies the challenge as disapproved, and add a test that a successful challenge still works after a failed one
