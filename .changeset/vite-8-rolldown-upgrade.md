---
---

chore: upgrade Vite 6 → 8 (Rolldown default bundler) and Vitest 3 → 4 across the workspace.

Dev-dependency / build-tooling change (Vite/Vitest are devDependencies), plus the
shared `@prosopo/config` migration for Rolldown/oxc and Vitest 4. No runtime package release.
