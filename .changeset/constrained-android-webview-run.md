---
---

Add a script that runs the Android WebView demo on deliberately weak emulated phones (low and mid memory, CPU and network limits) and records whether the widget loads, can be solved, survives a low-memory warning and recovers after the app is killed. The demo page now uses the always-pass test site key so the script can check a solve end to end.
