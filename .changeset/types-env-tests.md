---
"@prosopo/types-env": patch
---

Add type tests for the environment interfaces, and a unit test asserting the
package stays free of runtime exports. No behaviour changes: the package declares
types only.
