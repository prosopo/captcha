version: '3.8'
services:
  provider:
    image: prosopo/provider:v1.0.6
    build:
      context: ..
      dockerfile: ./docker/images/provider.dockerfile
    env_file:
      - ../.env.provider
    ports:
      - "9229:9229"
      - "80:80"
      - "443:443"
    tty: true
