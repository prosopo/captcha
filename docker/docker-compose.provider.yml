version: '3.8'
services:
  provider:
    image: prosopo/provider:latest
    build:
      context: ..
      dockerfile: ./docker/images/provider.dockerfile
    env_file:
      - ../.env.provider
    ports:
      - "9229:9229"
      - "80:80"
      - "443:443"
