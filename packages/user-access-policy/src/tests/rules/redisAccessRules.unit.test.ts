import { describe, test } from "vitest";

describe("redisAccessRules", () => {
	test("finds rules", () => {
		// todo reuse
	});

	test("finds rule ids", () => {
		// todo reuse
	});

	test("inserts rule", () => {});

	test("inserts rule with expiration", () => {});

	test("deletes rules", () => {});
});
