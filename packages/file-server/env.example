REMOTES="http://localhost:3001" # list of remote servers to proxy requests to (can be JSON array)
PORT=3000 # port to run the server on
RESIZE=128 # resize images to this size (in pixels)
PATHS="/path/to/your/images" # list of paths containing images (can be JSON array)
```