{
	"name": "@prosopo/http-blackhole",
	"version": "1.0.26",
	"main": "dist/vite/esm/index.js",
	"type": "module",
	"engines": {
		"node": "^24",
		"npm": "^11"
	},
	"scripts": {
		"build": "npm run build:esm",
		"build:esm": "NODE_ENV=${NODE_ENV:-development}; vite build --config vite.esm.config.ts --mode $NODE_ENV",
		"build:cjs": "NODE_ENV=${NODE_ENV:-development}; vite build --config vite.cjs.config.ts --mode $NODE_ENV",
		"build:types": "echo \"@prosopo/http-blackhole: no type declarations build (not supported for this package)\"",
		"build:tsc": "npm run build:tsc:esm",
		"build:tsc:esm": "tsc --build --verbose tsconfig.json",
		"build:tsc:cjs": "echo \"@prosopo/http-blackhole: no tsc cjs build (source uses import.meta, which tsc cannot emit as CommonJS; use build:cjs (vite) for CommonJS output)\"",
		"build:bundle": "echo \"@prosopo/http-blackhole: no bundle build (not supported for this package)\"",
		"build:bundle:es": "echo \"@prosopo/http-blackhole: no es bundle build (not supported for this package)\"",
		"build:bundle:iife": "echo \"@prosopo/http-blackhole: no iife bundle build (not supported for this package)\"",
		"build:bundle:webpack": "echo \"@prosopo/http-blackhole: no webpack bundle build (not supported for this package)\"",
		"start": "node dist/vite/esm/index.js"
	},
	"author": "",
	"dependencies": {
		"dotenv": "17.3.1"
	},
	"license": "Apache-2.0",
	"description": "",
	"devDependencies": {
		"@prosopo/config": "3.3.4",
		"@types/node": "24.10.13",
		"tsx": "4.20.3",
		"typescript": "5.9.3"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/prosopo/captcha.git",
		"directory": "packages/http-blackhole"
	}
}
